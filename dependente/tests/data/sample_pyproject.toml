[build-system]
requires = ["setuptools>=45", "setuptools_scm>=6.2", "wheel"]

[project]
dependencies = [
    "click >= 8.0.0, < 9.0.0",
    "rich >= 9.6.0, < 11.0.0",
    "tomli >= 1.0.0, < 3.0.0",
]

[project.optional-dependencies]
jupyter = ["nbformat >= 5.1",]
